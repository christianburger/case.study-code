import { DashboardLaunchpad } from './dashboard-launchpad.model';

describe('DashboardLaunchpad', () => {
  it('should create an instance', () => {
    expect(new DashboardLaunchpad()).toBeTruthy();
  });
});
